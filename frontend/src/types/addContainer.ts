export type addContainer = {
    port: string;
    destination: string;
    bl: string;
    ncontainer: string;
    estimatedDate: string;
}

export type addSeals = {
    port: string;
    destination: string;
    bl: string;
    estimatedDate: string;
}