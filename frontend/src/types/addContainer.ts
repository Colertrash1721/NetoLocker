export type addContainer = {
    port: string;
    destination: string;
    bl: string;
    ncontainer: string;
}

export type addSeals = {
    port: string;
    destination: string;
    bl: string;
}